// Package orch is the .orch / plano DSL compatibility layer: it compiles sources into the canonical
// model (internal/deploy/v1alpha1). Use [Module] for a process-wide [*Orch]; scheduling and runtime consume only v1alpha1.App.
package orch
