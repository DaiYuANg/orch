[package]
name = "runner"
version.workspace = true
edition.workspace = true

[dependencies]
tokio = { workspace = true, features = ["full"] }

zbus = { version = "3", features = ["tokio"] }
zvariant = "3"

bollard = { version = "0.19.1",features = ["tokio-stream"] }

tokio-fs = "0.1"