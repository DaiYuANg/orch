# Local Raft Cluster

The Go rewrite will use `github.com/hashicorp/raft` for consensus and will add
local multi-process cluster helpers in a future iteration.

## Status

This document is currently a placeholder while the raft-backed server wiring
and cluster tooling are implemented in Go.
