apiVersion: warden.arcgolabs.io/v1alpha1
kind: App
metadata:
  name: vagrant-smoke
  namespace: default
workloads:
  - name: smoke-worker
    kind: worker
    runtime: docker
    run:
      artifact:
        image: busybox:1.36
      exec:
        command: ["sh", "-c"]
        args: ["while true; do sleep 3600; done"]
    scheduling:
      preferredNodes:
        - node1
